FROM mysql:latest

COPY  ./job_application.sql /docker-entrypoint-initdb.d/ 

ENV MYSQL_USER=${USER} MYSQL_ROOT_PASSWORD=${MYSQL_ROOT_PASSWORD} MYSQL_PASSWORD=${MYSQL_PWD} MYSQL_DATABASE=${MYSQL_DATABASE} MYSQL_HOST=${MYSQL_HOST} TABLE1=${TABLE1} TABLE2=${TABLE2}